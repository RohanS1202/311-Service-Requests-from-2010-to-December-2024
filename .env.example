# Copy this file to `.env` and fill in values before running ingestion or the app
# Keep this file checked in; do NOT check in .env with secrets.

SOCRATA_APP_TOKEN=your_socrata_app_token_here
APP_ENV=dev

# Optional overrides
#STREAMLIT_MAX_ROWS=20000
